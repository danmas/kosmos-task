# Привет, Kosmos! .kosmos.md

**Статус:** in progress
**Прогресс:** 0%
**Последнее обновление:** 2026-06-30

## Краткое summary (пишется в конце)

(пусто)

## Цель

Создать простейшую демонстрацию формата .kosmos.md — вывести приветственное сообщение и показать, как runner автоматически обновляет файл.

## Задача 1: Вывести приветствие

### Шаг 1: Вывести "Привет, Kosmos!"
- [ ] Выполнено
 ```js executable
      const greeting = "Привет, Kosmos!";
      const timestamp = new Date().toLocaleString("ru-RU");
      console.log(`${greeting} (${timestamp})`);
      console.log(`Формат .kosmos.md — это живой документ!`);
```
Ожидаемый результат: Появится приветственное сообщение с датой и описанием формата

Результат:
(пусто)

Верификация:
- [ ] пройдена.

### Шаг 2: Показать математику
- [ ] Выполнено
 ```js executable
      const numbers = [1, 2, 3, 4, 5];
      const sum = numbers.reduce((a, b) => a + b, 0);
      const avg = sum / numbers.length;
      console.log(`Числа: ${numbers.join(", ")}`);
      console.log(`Сумма: ${sum}`);
      console.log(`Среднее: ${avg}`);
```
Ожидаемый результат: Будут выведены числа, их сумма и среднее значение

Результат:
(пусто)

Верификация:
- [ ] пройдена.
